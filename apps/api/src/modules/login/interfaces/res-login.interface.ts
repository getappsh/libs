export interface IResDate {
  access_token: string;
  expires_in: 1800,
  refresh_expires_in: number,
  refresh_token: string,
  token_type: string,
  'not-before-policy': number,
  session_state: string,
  scope: string
}